#ifndef CBPP_GRAPHICS_H
#define CBPP_GRAPHICS_H
/*
#include "gl/gl40.h"

namespace cbpp {
    const char* GetShaderClassExtension(GLenum iClass);

    class CShaderPipe {
        GLuint m_iProgram;

        GLuint CompileShader(const char* sName, GLenum iShaderClass, char* sBuffer);

        public:
            CShaderPipe() = default;
            CShaderPipe(const char* sVtx, const char* sFrag, const char* sGeom = NULL);

            bool CompilePipeline(const char* sVtx, const char* sFrag, const char* sGeom = NULL);
    };
}
*/
#endif
